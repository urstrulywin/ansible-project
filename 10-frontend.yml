- name: configure frontend server
  hosts: frontend
  become: yes
  tasks:
    - name: install nginx
      package:
        name: nginx
        state: present

    - name: remove default nginx directory (html dir)
      file:
        path: /usr/share/nginx/html/
        state: absent

    - name: delete frontend.zip in /tmp
      file:
        path: /tmp/frontend.zip
        state: absent

    - name: download frontend zip file
      get_url:
        url: https://roboshop-artifacts.s3.amazonaws.com/frontend-v3.zip
        dest: /tmp/frontend.zip

    - name: add default nginx directory (html dir)
      file:
        path: /usr/share/nginx/html/
        state: directory

    - name: unzip frontend file
      unarchive:
        src: /tmp/frontend.zip
        dest: /usr/share/nginx/html/
        remote_src: yes # the zip file is on the remote target host (frontend). ansible server is local here.

    - name: remove default nginx conf
      file:
        path: /etc/nginx/nginx.conf
        state: absent

    - name: copy our nginx.conf.j2
      template:
        src: nginx.conf.j2
        dest: /etc/nginx/nginx.conf

    - name: enable and restart nginx
      service:
        name: nginx
        state: restarted
        enabled: yes
